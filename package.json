{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.22.1",
    "ts-standard": "^12.0.2"
  },
  "scripts": {
    "build:demo": "lerna run build:demo",
    "postbuild:demo": "mkdir demo-dist && cp -r packages/**/demo-dist/* ./demo-dist"
  },
  "workspaces": [
    "packages/*"
  ],
  "version": "0.1.0"
}
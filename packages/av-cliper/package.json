{
  "name": "@webav/av-cliper",
  "version": "0.1.3",
  "private": false,
  "repository": "https://github.com/hughfenghen/WebAV",
  "type": "module",
  "types": "dist/av-cliper.d.ts",
  "module": "dist/av-cliper.js",
  "scripts": {
    "dev": "vite demo",
    "test": "vitest",
    "test:coverage": "vitest run --coverage",
    "buildxxx": "rm -rf dist && tsc -p build.tsconfig.json --outDir dist",
    "prepublishOnly": "yarn build",
    "preview": "vite preview"
  },
  "files": [
    "dist"
  ],
  "devDependencies": {
    "@vitest/coverage-istanbul": "^0.29.8",
    "jsdom": "^21.1.1",
    "ts-standard": "^12.0.2",
    "typescript": "^4.9.3",
    "vite": "^4.2.0",
    "vitest": "^0.29.7"
  },
  "dependencies": {
    "@webav/av-canvas": "^0.1.3",
    "mp4box": "^0.5.2"
  }
}
{
  "name": "@webav/av-canvas",
  "version": "0.1.0",
  "private": false,
  "repository": "https://github.com/hughfenghen/WebAV",
  "type": "module",
  "types": "npm-dist/index.d.ts",
  "module": "npm-dist/index.js",
  "scripts": {
    "dev": "vite demo",
    "test": "vitest",
    "test:coverage": "vitest run --coverage",
    "build": "rm -rf npm-dist && tsc -p build.tsconfig.json --outDir npm-dist",
    "prepublishOnly": "yarn build",
    "preview": "vite preview"
  },
  "files": [
    "npm-dist"
  ],
  "devDependencies": {
    "@vitest/coverage-istanbul": "^0.29.8",
    "jsdom": "^21.1.1",
    "ts-standard": "^12.0.2",
    "typescript": "^4.9.3",
    "vite": "^4.2.0",
    "vitest": "^0.29.7"
  }
}